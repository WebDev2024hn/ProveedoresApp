<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:ProveedoresApp.ViewModels"
    x:Class="ProveedoresApp.Views.ProveedoresPage">

    <ContentPage.BindingContext>
        <vm:ProveedorViewModel />
    </ContentPage.BindingContext>

    <VerticalStackLayout Padding="15" Spacing="10">

        <Entry Placeholder="Nombre" Text="{Binding Nombre}" />
        <Entry Placeholder="Empresa" Text="{Binding Empresa}" />
        <Entry Placeholder="Telefono" Text="{Binding Telefono}" />
        <Entry Placeholder="Email" Text="{Binding Email}" />
        <Entry Placeholder="Direccion" Text="{Binding Direccion}" />

        <Button Text="Agregar Proveedor"
                Command="{Binding AddProveedorCommand}" />

        <CollectionView ItemsSource="{Binding Proveedores}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border Stroke="Gray"
                            StrokeThickness="1"
                            Padding="10"
                            Margin="5"
                            StrokeShape="RoundRectangle 10">
                            
                        <VerticalStackLayout>
                            <Label Text="{Binding Nombre}" FontAttributes="Bold"/>
                            <Label Text="{Binding Empresa}" />
                            <Label Text="{Binding Telefono}" />

                            <Button Text="Eliminar"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:ProveedorViewModel}}, Path=DeleteProveedorCommand}"
                                    CommandParameter="{Binding .}" />
                        </VerticalStackLayout>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </VerticalStackLayout>
</ContentPage>